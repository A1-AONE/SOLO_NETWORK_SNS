import 'package:flutter/material.dart';

class SetpageOneNickname extends StatelessWidget {
  const SetpageOneNickname({super.key});

  @override
  Widget build(BuildContext context) {
    return Column(
      children: [
        // 
      ],
    );
  }
}